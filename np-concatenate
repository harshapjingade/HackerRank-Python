import numpy
n, m , p = map(int,input().split())
narr,marr=[],[]
for i in range(n):
    narr.append(numpy.array(input().split(),int))
for j in range(m):
    marr.append(numpy.array(input().split(),int))
print(numpy.concatenate((narr,marr),axis=0))
